---
publish: true
created: 2025-09-16T05:30:53.967Z
modified: 2026-09-14T11:08:52.443Z
---

# 奇怪小点

- 发现自己计组愚蠢的其中ld指令没画data memory
- 注意一个bit/byte
- 默认没有dirty bit
- Memread/write都不能是x（因为会影响后续的cache读取）

# 简单的英语学习

sustains 3 billion instructions per second：1s执行3billion
**Backplane bus**：高速机箱内部背板，速度快，连接Memory和CPU

# 计算机构成分类

- 内存+处理器
- hardware: CPU, memory, i/o
- ISA（instruction set architecture）: 分离上下层，影响复杂性
- 评价标准：响应时间，吞吐率（total work done per unit time），etc……
  - **Throughput / Bandwidth** 单位时间内完成的任务数量
  - Elapse time>CPU time: total response time(分别从用户/计算机视角)(后者是单独的处理时间)
    包括processing, I/O, OS overhead（操作系统开销）. idle time（空闲时间（计算机空转，应用率低））
    - ==\$1 ps=1\*10^{-12}s\$==
    - $GHz=10^9$
    - $KiB=2^{10}B$
      ==K M G T P E Z Y==
  - MIPS: Millions of Instructions Per Seconds
    $MIPS=\frac{Clock rate}{CPI*10^6}$
  - 转换：CPI:clock ==cycle== per instruction
    $CPU time =\frac{Instrution Count*CPI}{Clock Rate}$
    relative frequency视作$\frac{Instruction Count t_i}{Instruction Count}$
    Amdahl's law: $T_{improved}=\frac{}{}$
- 集成电路芯片
  ![[图源/图源1-7.30/Pasted image 20250918134612.png]]
  最后一个式子基于经验观察得到，其中指数实际上与加工步骤数量有关
- 内存
  ![[图源/图源1-7.30/2023-06-23_17-12-24.png]]

## ALU

1bit的ALU串联而成多bit的ALU
一点额外的结构：overflow：用最后（第63位为符号位）的carryin和carryout异或得到：说明in和out不同，两正的负或者两负得正，均发生了溢出；set（最后位）和less（比较结果rd）
优化：如果做减法或者 `slt` 的话，需要将 `Binvert` 和 ALU0 的 `CarryIn` 设成 `1` ，如果是加法的话这两个信号都是 `0` ；其他运算用不到这两个信号。因此这两个信号始终取值相等，我们可以将这两个信号合并为一个，称之为 `Bnegate` 。

### adder

更快的加法：使用预测/并行计算

### multiplier

V1:普通的128位加法
V2:改成64位加法之后一起右移
V3把一个乘法的放在原本被右移的位置
一种改进的乘法方法是 Booth's Algorithm。

#### Booth's Algorithm

在我看来，Booth's Algorithm 的主要思想在于，减少乘数中“`1`”的数量。具体来说，在我们原先的乘法策略中，每当乘数中有一位 `1` 时，都会需要一个加法（将位移后的被乘数累加到结果中）。但是，如果乘数中有较长的一串 `1` 时，我们可以将它转化为一个大数字减一个小数，如：`00111100 = 01000000 - 00000100`，此时 4 个 `1` 变为 2 个 `1`，我们就可以减少做加法的次数。这也是为什么 xyx 说有点像差分了。
[![](https://xuan-insr.github.io/computer_organization/2023-06-23_13-29-37.png)](https://xuan-insr.github.io/computer_organization/2023-06-23_13-29-37.png)

那么在实际执行过程中，Booth's Algorithm 主要遵循两位比特的模式进行操作。

[![](https://xuan-insr.github.io/computer_organization/2023-06-23_13-38-32.png)](https://xuan-insr.github.io/computer_organization/2023-06-23_13-38-32.png) [![](https://xuan-insr.github.io/computer_organization/2023-06-23_13-46-15.png)](https://xuan-insr.github.io/computer_organization/2023-06-23_13-46-15.png)

> 对于上面这张图的理解，稍微做一些解释。它实际上的表示是挪用了 V3 的乘法器。product 一列代表的是 product 寄存器，左一半一开始置 0，右一半一开始存放的是乘数，而上面提到的将被乘数加减累计到结果中的操作，也是对于左半部分来说的（**换句话来说就是用 V3 做 Booth's**）。需要提醒的是，在 Booth's Algorithm 中，两位识别是从第一位开始的，此时第一位作为两位中的高位，低位为 0，而后在下一轮中第一位作为低位，第二位作为高位，以此类推。

有意思的事出现了，Booth's Algorithm 也能应用于负数。仔细想想就会发现其实很合理，对于正数，其最高位一定是 `0`，因此不管怎么样它最终都会匹配到一个 `01` 的模式，来给一个 `1111..` 收尾。但是负数不然，负数的最高位是 `1`，这也意味着它最终不会执行所谓的 `01` 对应的加法操作来给 `111...` 收尾。换句话来说，它好像“亏”了一个在下一位的加法。而亏空的想法和补码的算法是完全一致的（即全部是1的补码）。即有符号数正数前面补0，负数补码全部补1来保持不变。这和减法一样。

### Division

shift right（仅左半边的余数右移，右半部分的商不动）/shift left:商左移/被除数左移
余数的符号跟被除数/其余先按正数处理，最后再加

### 浮点数！

用IEEE754标准描述
单精度：1（符号）+8（指数）+23（小数）
双精度：1+11+52
指数位为111111……且分数位为000：$\infty$
不为：NaN
0000                        000:0
0000                   不为：underflow
小对大：因为大数的前位比小数的后位更为重要
==注意点==：

1. 略去的默认位1
2. 默认的偏置位（127/1023）一半要减去
   四舍五入的方式：guard：在lsb后的第一位
   round：在guard后的第一位
   units in the last place(ulp)误差占最小精度值的百分比
   _四舍五入：0.5ulp_
   sticky: 紧跟 Round 之后，表示“被截去的所有剩余位的 OR”
   100（三位GRS）:则rounding to nearest even

## instructions

risc<->cisc:对比 简单一条可以实现复杂指令：存储空间少，处理较快
一个word32bit，64bits：doubleword
各个寄存器基本用途
![[图源/图源1-7.30/Pasted image 20251016152529.png]]
RISC-V汇编指令
![[图源/图源1-7.30/Pasted image 20251016152811.png]]
不能直接加载A\[i]，因为变量不能直接访问，要先手动加载偏移量
![[图源/图源1-7.30/Pasted image 20251016155001.png]]
逻辑右移<->算数右移：右边用于有符号整数（符号补位）
RISC-V实际用16字节的栈，但由于MIPS迁移教学，书/考试/作业一般都偏8byte
使用atomic指令来处理两个processor同时处理一个memory的问题/避免过长的时钟周期，会降低总体的运行效率

#### controller

7（其它7个控制指令：ALUSrc，Memwrite, Memread, MemtoReg, jump, ALUoperation, regWrite）+4（ALU指令）==要记==

## processor（pipeline!

PC时钟信号在下降沿输入

#### hazard：冒险 有害的

- stall：bubble，等待时间

##### 分类/解决方法

- control：需要等待上一步的指令完成：跳转etc
  通过预测来降低预测错误的概率-51%赌场即赚
  静态/动态预测：经典模型比如for；reg里面存储常用键值
  _逆向传递：送回branch的mux、write reg的信号_
  提高效率
  cache：迅速送入上层存储器
  1\. 把alu挪到前面
  2\. 加一个寄存器的比较器
  错误的判断清3条指令-其余control寄存器清零就好（flush）
  尝试采用动态方法：储存历史上跳没跳的指令lower part，错了就flush
  2-bit predictor: 错两次就跳（predictor/target两种）
- data：等待数据读写完成
  方法：code scheduling（让load先靠前）\&forwarding 使用旁路来传递数据
  forward当且仅当前（两个周期）后rs、rd相等且regwrite=1
  若两个周期前后都冲突：先forward近的那一个；mem/wb forward当且仅当前一个不forward

```
if (MEM/WB.RegWrite
and (MEM/WB.RegisterRd ≠ 0)
and not(EX/MEM.RegWrite and (EX/MEM.RegisterRd ≠ 0)
and (EX/MEM.RegisterRd = ID/EX.RegisterRs1))
and (MEM/WB.RegisterRd = ID/EX.RegisterRs1)) ForwardA = 01
```

```
Load Use Data hazard：一定有一个bubble；否则最多有一个bubble停周期
```

- structure：结构正在执行其他的指令：用时钟上下沿分开读入解决冲突
- load-use hazard用当且仅当发现是load&&前rd==后rs
  IF\ID\WB：instruction fetch/decode（存的是原始32位指令）

##### 处理

- stall

1. force control values in ID/EX to 0(所有的作用都毫无效果nop: _no operation_)
2. 阻止PC和IF/ID（锁住，不重读IM（可能会读丢））的变化：重新decode-重取指令/寄存器-MEM read data for ld

- 编译器帮助减少hazard和stall（bubble）次数

#### Exceptions and Interrupts

##### Exception

CPU之内

1. SPEC: supervisor exception program counter存一下PC
2. SCAUSE: supervisor exception Cause Register：存可能的问题
3. jump to handler: 调用修复程序
   可行：跳回pc
   不可行：报错，让程序员调
   有很多exception：看最早的

##### Interrupt

来自外界I/O: 采用被动发中断的方式处理
向量值中断
采取各个分量对应的措施/handler
不精确的预测

#### ILP Instruction-Level Parallelism

1. 更深的pipeline
2. 多个ins同时执行(不过必须要不同类型：ALU/branch; load/store)
   多加了几个硬件，避免结构hazard
   静态：issue slot
   ISA support 类似于多条指令放到同一个周期 very long word
   ld判定变得复杂：pad with nop
   动态：CPU检测重排
   实时判断是否符合条件
   加一个commit unit
3. speculation: guess that is right  静动态都用
   compiler/buffer努力
   调顺序来优化延迟
   loop unrolling: 采用不同的regs来同步展开cycle的instruction，避免冲突，加快效率

#### 注意点&&重点更改

加入reg
下降沿写入
关注吞吐量而不是latency

# Chap 5

取的地址位数只会到byte
注意复制单元是block，一个word一般4位
注意地址是一体的！
下：flash和disk不是内存（memory）
上：cache
最高位：block address
分为tag/index和byte offset
block placement:

1. fully associative: Block can go anywhere in cache，但是不好找
2. Set associative: n-Set associative: 有n个框，每个可以平等地按规定前往组内的任意一个block。看tag判定哪个是处理器要取的
3. Direct mapped：只能去一个地方

| write back    | control bits：dirty bits | write-allocate | 先把内存加载到Cache里面再写入 |
| ------------- | ----------------------- | -------------- | ----------------- |
| write through |                         | write around   | 直接写入内存            |
| 读时            |                         | 写时             |                   |

冷启动：第一次进入的时候一定会miss，从memory中加载到cache里面
竞争策略：
LRU：放回最少用的，但是成本较高，需要统计累计次数
虚拟内存：安全性提升
Page table：进行虚拟页地址和真实页地址的一一对应
TLB作为页表的buffer
reference bit: LRU 定期清零
Fast and Big：多级缓存：Regs-Cache(On-chip)-Cache(SRAM)
这里On-chip指位置在CPU-chip旁边，访问速度快；SRAM指静态随机存储器，使用技艺

# Chap 6: I/O

通过raid来提高容灾能力

1. 单纯复制
2. 没讲
3. 一节一起
4. 一个block一起
5. raid disk错开
   北桥：快速，集中memory部分
   南桥：慢速
   controller：管理硬件，在CPU和外设之间调度
   disk是磁盘外设，memory是主存
   seek+rotation+read三段时间组成

# lab5

有一堆的数据通路
SCPU\_ctrl 需要额外补上对 bne 指令的处理
CPU：SCPU
CPU1:

## 命令行

| **类别**                                      | **示例**                  | **功能**    |
| ------------------------------------------- | ----------------------- | --------- |
| 项目操作                                        | open\_project my.xpr     | 打开工程      |
| close\_project                               | 关闭工程                    |           |
| IP管理                                        | update\_ip\_catalog       | 更新IP目录    |
| ipx::package\_project                        | 打包IP                    |           |
| ipx::save\_core                              | 保存IP                    |           |
| 运行控制                                        | launch\_runs synth\_1     | 启动综合      |
| wait\_on\_run synth\_1                         | 等待综合完成                  |           |
| launch\_runs impl\_1 -to\_step write\_bitstream | 直接跑到生成比特流               |           |
| 查询                                          | get\_ports / get\_cells   | 查看端口或逻辑单元 |
| 报告                                          | report\_utilization      | 资源利用率     |
| report\_timing\_summary                       | 时序报告                    |           |
| 文件操作                                        | file delete -force .Xil | 删除目录      |
| file mkdir new\_folder                       | 创建文件夹                   |           |
| launch\_simulation -type behavioral          |                         |           |
