【token】词法分析之后还会存在的东西
【nontokens】可能词法分析之后就没了
语义错误：和上下文联系的时候有错；类型不匹配；少参多参
【DFA/确定有限自动机】两个圆圈：结束状态；终结状态接受到没标的状态可能表示要进入新的状态，非终结状态接了就是错了，应该换一个
三个光标同时维护当前的状态| 上T，下倒T处理完的-上一次处理的终结状态-现在在看
- 任何一个dfa一定有一个最小化的状态并且最小化之后都可以到达这个状态
- 尝试先所有都合成同一个，有冲突再说？
冲突
1. 如果两个一个是终结一个是非终结，那么是冲突的
2. 经过同一个变换没有到达同一个结果或者等价的结果
3. 不能接受相同的变换（比如有一个不能进行那个变换）
【NFA/非确定有限状态机】
- 允许一个状态未经任何输入就转到另一个状态
- 允许一个状态经过两个相同的转化到不同的状态
- ==有点没看懂==
- 词法分析比较困难，所以需要转化为DFA
	- 状态集合，枚举每种转化方式
	- $\epsilon \space closure$：不经过任何输入就可以到达状态的集合
	- 原来式终结节点的转化之后包含这些节点的也是终结节点
【正则表达式】
- longest match：最长匹配，优先匹配相似程度最高的
- 优先匹配能匹配到的
- .表示除了换行符外的所有单个字符
【词法分析】其实就是一个函数，调用之后返回第一个成立的单词，之后反复调用这个函数