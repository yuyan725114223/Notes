# 概论
## 人工智能分类
符号主义：情况太复杂（
连接主义：大量简单单元+权重学习 *权重：神经元之间的连接紧密度* 机器学习
行为主义：通过传感器反馈实现模型的优化
## 机器学习分类
监督学习：分类问题 跟着老师学
无监督学习：聚类问题 自己分析研究
强化学习：决策问题 环境探索中积累经验
半监督学习
自监督学习：比较相似度/正负样本
深度学习：好的特征可以使用通过学习的方法自动学习得到（浅层分类需要好的特征提取器）
# 实验
## 1. mnist手写数字识别
tensor操作：张量～多维数组/矩阵pytorch
1. 读取并建出train set和valid test x数据，y标签
	用gpu 因为都有矩阵乘法且性能好～
	1. cuda确认：
~~~
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
torch.cuda.is_available()
~~~
	2. cuda方法
~~~
x_0_gpu = x_0_tensor.cuda()
x_0_gpu.device
~~~
2. 处理：将PIL图像转化为3维/多维tensor便于使用（3维是因为黑白，彩色则更多：颜色三原色合成）
	tensor大小指\[C,H,W]
	独热码：类似于解码器，输出只有1位为1的0，1序列
3. 像素值归一来让计算更稳定
4. 可以用`to_pil_image` 函数将 `C x H x W` 格式的张量转换回 PIL 图像。
~~~
image = F.to_pil_image(x_0_tensor)
plt.imshow(image, cmap='gray')
~~~
5. 数据加载器：dataloader，分批装入batch_size：使用较小的数据批次（batch）对模型训练更有效
6. 基本构成
	1. 激活函数 (σ)：每个神经元对其输入的加权和加上偏差应用一个激活函数。常见的激活函数包括 S 型函数、ReLU（修正线性单元）和 tanh（双曲正切）。激活函数引入非线性，使网络能够建模复杂的关系。
	2. 中间隐藏层：用来做特征提取
	3. 输入展平：默认(1,-1)从第二dim展到最后一个dim
~~~input = torch.randn(32, 1, 5, 5)
# With default parameters
>>>m = nn.Flatten()
>>>output = m(input)
>>>output.size()
torch.Size([32, 25])
# With non-default parameters
>>>m = nn.Flatten(0, 2)
>>>output = m(input)
>>>output.size()
torch.Size([160, 5])
~~~
## 2. asl美国手语字母识别（图片）
x_train.shape:识别变量各方面的维度
### 卷积神经网络
权重共享：即卷积核的权重分配是一致相同的
算总参数：输入层不计算入
输出层*(输入层+1) *1是线性组合内的bias*
### 归一化
### 正则化：适度惩罚来让模型避免过拟合
1. L1正则化：在mse中增加L1范数（权重向量参数中的绝对值之和）
	倾向于产生稀疏权重矩阵，特征更少
2. L2正则化：L2范数（平方和）
	避免参数过大，但不会变为0，有助于平滑模型
## 数据增强
出于数据的不足和收集的麻烦，对已有的数据做一些预处理来节约成本
	处理：比如
	1. 几何变换：旋转，翻转，裁剪，变形，缩放，etc
	2. 颜色变换：（高斯）噪声，模糊，颜色变换/扰动，擦除填充etc
## 迁移学习
目标不同或源数据不同
任务：分类，回归，识别
- 迁移什么？
1. 实例 *e.g.增加一类*
2. 特征：同构/异构迁移学习：源域和特征域的分布是否一致
3. 模型 共享一些模型参数 *即可能类相同，种不相同*
image = image.unsqueeze(0) # Turn into a batch，即在第一维加上一个1，方便送进模型
## RNN：循环神经网络
容易梯度爆炸/消失
	使用：LSTM(long short-term memory)长的短期记忆
	加入了forget gate
词嵌入：把自然语言的词映射成低维空间向量
BM25：用来计算文本和文档的相关性评分
用google BERT：预测一系列单词之后的新的单词，句子后新的句子
1. 分词器：可以一次性[编码](https://huggingface.co/docs/transformers/main_classes/tokenizer#transformers.PreTrainedTokenizer.encode)多段文本
## 图像生成模型
学习训练集的样本分布，在样本分布中随机采样出一个点
密度估计问题：在 **不知道数据真实分布的情况下，想要估计它的概率分布函数（PDF）**
生成序列数据：第n个像素根据前n-1个像素计算概率 component by conponent/从坐上角开始产生像素
	慢且效果不好。狗狗的部位拼接奇怪
